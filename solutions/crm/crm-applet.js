// Applet entry: registers both CRM comps (crm + productMap) so navTo() between them resolves.
// Deploy target: uploadRoomApplet({ roomId, entryPath: '@solution/crm/crm-applet.js', entryCompFullId: 'react-comp<react>crm' }).
import './crm.js'
import './product-map.js'
