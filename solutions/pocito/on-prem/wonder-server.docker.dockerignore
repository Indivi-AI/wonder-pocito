# On-prem context keeps solutions/.jb6/tests (live-repo applets import them) - unlike the root .dockerignore.
.git
node_modules
files
tmp
cloud-services/.local
.cache
*.log
wonder.zip
# deployment tooling never runs inside the app container; keeps .ts + mock datasets out of the whitened image
solutions/pocito/on-prem
