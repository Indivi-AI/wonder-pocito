When creating or editing a verified report, publish the studio applet once — `uploadRoomApplet({ roomId: 'comaxDemo', entryPath: '@wonder-admin/comax/demo/reports-studio-demo.js', entryCompFullId: 'react-comp<react>comaxReportsStudioDemo' })` — then display it via the room-applet URL on localhost (serves liveRepo, no re-upload to see edits): `http://localhost:3000/room/comax/applet/comaxReportsStudioDemo?ctx-reportId=<reportId>&logger=uiLogger,dbLogger`, and verify with `playwrightHarvest` — read the logs, not screenshots.
